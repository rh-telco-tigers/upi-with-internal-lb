apiVersion: v1
kind: Pod
metadata:
  name: recyler-pod
  namespace: openshift-infra
spec:
  activeDeadlineSeconds: 60
  restartPolicy: Never
  serviceAccountName: pv-recycler-controller
  containers:
    -
      name: recyler-container
      image: "quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:f3b16fb1ea070be814a631a70a664280ca18879b635fb012eb38584ab09f786e"
      command:
      - "/bin/bash"
      args:
      - "-c"
      - "test -e /scrub && rm -rf /scrub/..?* /scrub/.[!.]* /scrub/*  && test -z \"$(ls -A /scrub)\" || exit 1"
      volumeMounts:
        -
          mountPath: /scrub
          name: vol
      securityContext:
        runAsUser: 0
  volumes:
    -
      name: vol
